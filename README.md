# zenn-data-analytics

[`uv`](https://docs.astral.sh/uv/)が使用可能な状態で、`uv sync`を実行する。
そのあと、`scripts/`にあるスクリプトを順に実行する。

* `data.jsonl`: 匿名化処理を施したデータ
* `scripts/`: 図や表を生成するためのスクリプト
* `images/`: スクリプトを実行して生成された図が配置されているディレクトリ
